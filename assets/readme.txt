*Readme*

to test video upload:

1) Run the app locally as a common spring-boot application
2) Open fileUploadExample.html and upload a videofile, as an example the one which is in the /assets folder can be used
3) If upload is successful as a response you receive a direct video-url

Also, please check that access and vendor keys are properly set